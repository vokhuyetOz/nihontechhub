services:
  next-boilerplate:
    build:
      context: .
      dockerfile: dockerfile.development
    env_file:
      - .env
    ports:
      - ${PORT}:${PORT}
    # volumes:
    #   - .:/app:delegated
    #   - /app/node_modules
networks:
  default:
    driver: bridge
    name: next-boilerplate
